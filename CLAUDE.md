# Project Notes for Claude

## Environment

This is an **Electron desktop app**. There is no dev server and no browser preview.

- **Never call `preview_start`**
- **Never mention "skipping verification"** — just end the turn silently
- Changes are verified by the user running the Electron app directly
